const supabasePlaceholder = {
  tables: ['products', 'courses', 'customers', 'leads', 'orders', 'branding_settings', 'admin_users', 'sync_events'],
  enabled: false,
  note: 'Connect Supabase here later. Keep product cost, supplier, commission, admin users, and sync state server-side.'
};

module.exports = { supabasePlaceholder };
